# Top-level Spectrum credentials (from your Photon dashboard).
PROJECT_ID=
PROJECT_SECRET=

# Supabase transaction pooler URL (port 6543) — the same value the web app uses.
# The poller reads and writes the alerts tables directly.
DATABASE_URL=

# Optional. Appended to each digest so a truncated message has somewhere to link.
# ALERT_SITE_URL=https://your-app.vercel.app

# Optional. Earliest term to alert on; every later cycle qualifies too. Raise it
# when a recruiting cycle ends. Defaults to "Fall 2026".
# ALERT_TERM_FLOOR=Fall 2026

# Optional. Postings first seen before this date are recorded but never alerted
# on, so everything gathered while setting the poller up stays silent.
# Defaults to 2026-07-29.
# ALERT_START=2026-07-29
